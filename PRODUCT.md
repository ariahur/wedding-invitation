# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

주 하객은 **한국 하객**이다. 국문이 기본 언어이고, 영문(`/en`)은 소수의 해외 지인을 위한 보조 언어다.
다만 두 언어 중 어느 쪽도 레이아웃이 깨지거나 내용이 축약되어서는 안 된다 — 완전 동등하게 다룬다.

하객은 대부분 **카카오톡 등으로 받은 링크를 폰에서 한 번 열어보는 상황**이다.
그 한 번의 열람 안에서 (1) 날짜·장소를 확인하고, (2) 참석 여부를 회신하고, (3) 두 사람의 이야기를 읽는다.
예식 당일 이후에는 같은 링크로 돌아와 **자기가 찍은 사진을 부친다**.

부모님 세대(어르신) 하객이 상당한 비중을 차지한다. 폰 화면에서 무리 없이 읽히는 글자 크기와 대비가 필수다.

## Product Purpose

조준용·허다영 두 사람의 결혼식(2027년 2월 20일 15:00, 서울 그랜드힐컨벤션 1층 플로리아) 모바일 청첩장.

성공은 네 가지가 동시에 일어나는 것이다:

1. RSVP 응답이 제출된다 (식사 수량 등 실무에 직결).
2. 예식 후 하객이 찍은 사진이 접수된다.
3. 장소·시간·길찾기를 헤매지 않고 얻는다.
4. 끝까지 읽고 마음이 움직인다 — **결혼식에 오는 일이 "함께 여행을 떠나는 일"처럼 느껴진다.**

4번은 장식이 아니라 이 제품의 목적이다. 정보 전달만 하는 청첩장이면 실패다.

## Positioning

"비행기 티켓 같은 초대장." 결혼식 참석을 **탑승**으로, 하객을 **동승객**으로 다룬다.
이 은유는 껍데기가 아니라 제품의 구조 자체다 — Hero는 실제 보딩패스, RSVP 완료는 발권된 티켓,
사진 접수는 수하물 태그, 인트로는 공항 안내판이다. 노선(SYD → ICN)은 실제로 두 사람이 시드니에서
만나 서울에서 결혼한다는 사실에서 나온 것이지, 꾸밈이 아니다.

## Operating Context

- 유입 경로는 사실상 **메신저 링크 → 모바일 브라우저** 하나다. 실사용 폭은 320~430px 세로 화면이며,
  데스크톱은 고려 대상이 아니다.
- 라우팅은 `/ko`, `/en` 두 개뿐인 단일 페이지 스크롤. 섹션이 위에서 아래로 이어진다:
  Hero(보딩패스) → 우리 소개 → 타임라인 → 갤러리 → 오시는 길 → RSVP → 사진 수화물 접수 → 감사 인사.
- 사진 접수 카운터는 **시간에 따라 상태가 바뀐다**: 예식 전 `closed`(오픈 예정 안내 + D-day),
  당일 `open`(업로드 가능), 이후 `archived`(마감).
- RSVP는 재조회가 가능하다 (`RsvpLookupModal`) — 이미 제출한 하객이 자기 응답을 다시 확인할 수 있다.

## Capabilities and Constraints

**기능**
- 한/영 전환 (URL + 상단 토글), 문구는 전부 `src/data/translations.ts` 한 곳에 있다.
- RSVP 폼: 성함·연락처·참석 여부 필수, 동행 인원 등 조건부 필드. react-hook-form + zod 검증.
- 사진 수화물 접수: 다중 파일 업로드, 진행률 표시, 완료 시 수하물 태그 발급.
- 갤러리: 사진 방향(가로/세로)에 따라 블록 배치가 자동으로 정해진다. 배치 순서 = 파일명 순서.
- 연락처 모달(전화 링크), 주소 복사, 카카오맵 링크, 타임라인 이미지 모달.

**기술 제약**
- **Create React App(react-scripts 5) + TypeScript.** Next.js가 아니다 — 루트의 `app/`, `next.config.ts`,
  `eslint.config.mjs` 는 미사용 잔재이며 빌드에 포함되지 않는다.
- 서버가 없다. 백엔드는 **Google Apps Script 웹 앱 하나**(RSVP 저장 + 사진 업로드)이며
  `google-apps-script.js` 는 자동 배포되지 않는다 — 수정 시 편집기에 붙여넣고 재배포해야 한다.
- 환경변수 접두사는 `REACT_APP_` 만 유효하고, 값은 빌드 시 번들에 문자열로 박힌다 → 서버 시크릿 금지.
- 의존성 설치는 항상 `npm install --legacy-peer-deps` (react-scripts 5 + react-router-dom 7 peer 충돌).
- **테스트 파일이 없다.** 검증은 `npm run build` 하나로 한다 (타입 체크·린트 포함).
- 이미지는 `assets/originals/<그룹>/` → `npm run images` → `public/<그룹>/` + `imageManifest.json` 파이프라인을
  거친다. 원본은 저장소에 커밋하지 않는다. 코드에서는 `imageProps()` / `imageGroup()` 만 쓴다.
- 배포는 Vercel SPA (`vercel.json` rewrite로 모든 경로를 `index.html` 로).

**용어**
보딩패스 / 게이트 / 탑승 / 수하물 태그 / 카운터 / 동승객 — 하객에게 보이는 문구는 이 어휘를 쓴다.

## Brand Commitments

사용자가 명시적으로 구속력을 부여한 것:

- **보딩패스 은유를 유지한다.** 항공권·수하물 태그·게이트 안내판은 바꿀 수 없는 정체성이다.
- **어르신 가독성**을 지킨다. 폰에서 부모님 세대가 무리 없이 읽을 수 있는 글자 크기와 대비.
- **모바일 전용 폭(320~430px)** 을 실사용 환경으로 삼는다.
- **한/영 완전 동등.** 어느 쪽도 레이아웃이 깨지거나 내용이 축약되지 않는다.

색·폰트 팔레트와 그 근거는 `.claude/rules/theme-concept.md` 와 `THEME_COLORS.md` 에 이미 정의되어 있고,
섹션 레이아웃 규격은 `src/sections/CLAUDE.md` 에 있다. 이 문서들이 기존 시각 체계의 정본이다.

## Evidence on Hand

실제 자료만 있고, 지어낸 것은 없다.

- **실명·실제 사실**: 신랑 조준용(장남, 부 조웅일·모 김미정), 신부 허다영(장녀, 부 허윤·모 황영식).
  예식 2027.02.20 15:00, 그랜드힐컨벤션 1층 플로리아, 서울시 강남구 역삼로 607(대치동), Tel. 02-6964-7889.
  편명 표기 DA206, 노선 SYD → ICN.
- **사진**: `src/data/imageManifest.json` 기준 4개 그룹 — `gallery`(9장), `hero`, `about`, `timeline`.
  원본은 `assets/originals/` 에 있고 저장소에 없다.
- **타임라인**: 2013 / 2018 / 2019 / 2025 / 2027 실제 연도 이벤트.
- **없는 것 (절대 지어내지 말 것)**: 후기·추천사·통계·수상·언론 언급 같은 것은 존재하지 않고 필요하지도 않다.
  하객 수, 참석률 같은 수치도 아직 없다.

## Product Principles

1. **은유가 먼저다.** 새 UI 요소는 종이 인쇄물(티켓·태그·스탬프·안내판)로 성립하는 형태를 우선한다.
   보딩패스 세계관과 무관한 은유를 섞지 않는다.
2. **한 손, 한 번의 스크롤.** 모든 판단은 320~430px 세로 화면에서 엄지로 훑는 상황을 기준으로 한다.
3. **어르신이 읽을 수 있어야 아름다운 것이다.** 가독성과 미감이 충돌하면 가독성이 이긴다.
4. **두 언어는 하나의 제품이다.** 국문에서 성립하는 레이아웃은 영문에서도 성립해야 한다.
   문구는 `translations.ts` 밖으로 나가지 않는다.
5. **감동이 곧 기능이다.** 정보 전달은 최소 조건이고, "여행을 떠나는 느낌"을 남기지 못하면 미완성이다.

## Accessibility & Inclusion

부모님 세대 하객이 주요 사용자에 포함된다. 본문 글자 크기, 텍스트/배경 대비, 터치 타깃 크기를
그 기준으로 잡는다. 별도로 준수하기로 한 공식 표준(WCAG 등급 등)은 아직 정해지지 않았다.
