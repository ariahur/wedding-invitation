# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build

# 원본 사진 (장당 5~7MB). npm run images 로 public/ 에 웹용 사본을 만들어 쓴다.
# 원본 자체는 저장소가 아니라 별도(로컬/클라우드)에 보관할 것.
/assets/originals

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local
.env

npm-debug.log*
yarn-debug.log*
yarn-error.log*
